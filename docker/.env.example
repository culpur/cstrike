# CStrike v2 — Docker Stack Environment
# Copy to .env in the project root

# PostgreSQL
POSTGRES_DB=cstrike
POSTGRES_USER=cstrike
POSTGRES_PASSWORD=changeme-use-strong-password

# Redis
REDIS_PASSWORD=changeme-use-strong-password
